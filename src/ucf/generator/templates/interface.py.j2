"""Generated interface for use case: {{ usecase_name }}.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any

from ucf.testing.framework_actions import FrameworkActions


{% for dc in dataclasses %}
@dataclass(frozen=True)
class {{ dc.class_name }}:
{% for field in dc.fields %}
    {{ field.name }}: {{ field.type }}
{% endfor %}
{% if not dc.fields %}
    pass
{% endif %}


{% endfor %}
class {{ interface_class }}(ABC, FrameworkActions):
    """Interface with framework-provided actions inherited from FrameworkActions."""
{% if not (setup_methods or action_methods or verify_methods) %}
    pass
{% endif %}

    # ── State Setup (from requires) ──
{% for method in setup_methods %}

    @abstractmethod
    def {{ method.name }}(self{{ method.params_str }}) -> {{ method.return_type }}:
        ...
{% endfor %}

    # ── Actions (from steps) ──
{% for method in action_methods %}

    @abstractmethod
    def {{ method.name }}(self{{ method.params_str }}) -> {{ method.return_type }}:
        ...
{% endfor %}

    # ── Verifications (from postconditions + invariants) ──
{% for method in verify_methods %}

    @abstractmethod
    def {{ method.name }}(self{{ method.params_str }}) -> None:
        ...
{% endfor %}
