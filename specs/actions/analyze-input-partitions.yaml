kind: action
metadata:
  name: analyze-input-partitions
  version: "1.0"
  owner: ucf-framework
  tags: [completeness, analysis]

input:
  registry:
    type: object
    required: true
    description: loaded SpecRegistry with all specs

output:
  partition_coverages:
    type: array
    description: coverage record per input partition
  findings:
    type: array
    description: findings for uncovered partitions

errors:
  - status: 400
    code: NO_ACTIONS_LOADED
    condition: registry contains no action specs

reads:
  - resource: spec-registry
    fields: [actions, usecases]
