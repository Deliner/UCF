"""Standalone inventory reference adapter implementation.

This package deliberately imports only the Python standard library.  It is a
fixture implementation of the serialized inventory profile, not an in-process
UCF adapter.
"""
