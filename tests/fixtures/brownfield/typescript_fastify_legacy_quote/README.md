# Legacy Quote Service

This frozen application is a standalone TypeScript/Fastify service with no UCF
integration. It requires Node 22 and npm 10.

Run its native checks from this directory:

```text
npm ci --ignore-scripts --no-audit --no-fund
npm run build
npm test
```
