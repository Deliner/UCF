"""Generated interface for use case: shorten-url.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any

from ucf.testing.framework_actions import FrameworkActions


@dataclass(frozen=True)
class CreateShortLinkResult:
    code: str
    url: str
    expiry: str


class ShortenUrlInterface(ABC, FrameworkActions):
    """Interface with framework-provided actions inherited from FrameworkActions."""

    # ── State Setup (from requires) ──

    # ── Actions (from steps) ──

    @abstractmethod
    def action_create_short_link(self, original_url: Any, custom_code: Any) -> CreateShortLinkResult:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_content_creator_receives_shortened_url(self) -> None:
        ...

    @abstractmethod
    def verify_shortened_url_is_ready_to_use_immediately(self) -> None:
        ...

    @abstractmethod
    def verify_shortened_url_redirects_to_original_page(self) -> None:
        ...

    @abstractmethod
    def verify_creation_is_recorded_with_timestamp(self) -> None:
        ...

    @abstractmethod
    def verify_required_inputs_validated(self) -> None:
        ...
