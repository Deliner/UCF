"""Generated interface for use case: redirect-to-original.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any

from ucf.testing.framework_actions import FrameworkActions


@dataclass(frozen=True)
class LookupUrlResult:
    url_record: Any


@dataclass(frozen=True)
class AcquireClickLockResult:
    lock_id: str
    acquired: bool


@dataclass(frozen=True)
class IncrementClicksResult:
    new_count: int


@dataclass(frozen=True)
class ReleaseClickLockResult:
    released: bool


@dataclass(frozen=True)
class RedirectResult:
    redirected: bool


class RedirectToOriginalInterface(ABC, FrameworkActions):
    """Interface with framework-provided actions inherited from FrameworkActions."""

    # ── State Setup (from requires) ──

    # ── Actions (from steps) ──

    @abstractmethod
    def action_lookup_url(
        self,
        slug: Any,
    ) -> LookupUrlResult:
        ...

    @abstractmethod
    def action_acquire_click_lock(
        self,
        resource: Any,
        key: Any,
        timeout: Any,
    ) -> AcquireClickLockResult:
        ...

    @abstractmethod
    def action_increment_clicks(
        self,
        slug: Any,
    ) -> IncrementClicksResult:
        ...

    @abstractmethod
    def action_release_click_lock(
        self,
        lock_id: Any,
    ) -> ReleaseClickLockResult:
        ...

    @abstractmethod
    def action_redirect(
        self,
        target_url: Any,
        status_code: Any,
    ) -> RedirectResult:
        ...

    @abstractmethod
    def action_return_404(
        self,
        data: Any,
        format: Any,
    ) -> None:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_visitor_is_redirected_to_original_url(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_click_count_is_incremented_by_1(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_redirect_uses_302_found_status(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_required_inputs_validated(
        self,
    ) -> None:
        ...
