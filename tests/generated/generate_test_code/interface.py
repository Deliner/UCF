"""Generated interface for use case: generate-test-code.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any


@dataclass(frozen=True)
class LoaderContext:
    registry: Any
    loaded_count: int
    load_errors: list[Any]


@dataclass(frozen=True)
class GenerateResult:
    interface_path: str
    orchestrator_path: str
    impl_path: str
    files_written: int


class GenerateTestCodeInterface(ABC):

    # ── State Setup (from requires) ──

    @abstractmethod
    def setup_loader(self) -> LoaderContext:
        ...

    # ── Actions (from steps) ──

    @abstractmethod
    def action_generate(self, usecase: Any, registry: Any, output_dir: Any) -> GenerateResult:
        ...

    @abstractmethod
    def action_render_result(self, data: Any, format: Any) -> None:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_for_each_usecase_interface_py_test_orchestrator(self) -> None:
        ...

    @abstractmethod
    def verify_impl_py_stubs_are_created_only_if_they_do_not_alre(self) -> None:
        ...

    @abstractmethod
    def verify_generated_code_is_deterministic_given_the_same_spe(self) -> None:
        ...
