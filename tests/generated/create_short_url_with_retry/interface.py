"""Generated interface for use case: create-short-url-with-retry.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any

from ucf.testing.framework_actions import FrameworkActions


@dataclass(frozen=True)
class ValidateUrlResult:
    is_valid: bool
    error_message: str


@dataclass(frozen=True)
class GenerateSlugResult:
    slug: str


@dataclass(frozen=True)
class CheckExistsResult:
    exists: bool


@dataclass(frozen=True)
class StoreUrlResult:
    url_id: str
    short_url: str


class CreateShortUrlWithRetryInterface(ABC, FrameworkActions):
    """Interface with framework-provided actions inherited from FrameworkActions."""

    # ── State Setup (from requires) ──

    # ── Actions (from steps) ──

    @abstractmethod
    def action_validate_url(
        self,
        url: Any,
    ) -> ValidateUrlResult:
        ...

    @abstractmethod
    def action_generate_slug(
        self,
        length: Any,
    ) -> GenerateSlugResult:
        ...

    @abstractmethod
    def action_check_exists(
        self,
        slug: Any,
    ) -> CheckExistsResult:
        ...

    @abstractmethod
    def action_store_url(
        self,
        slug: Any,
        original_url: Any,
        created_by: Any,
    ) -> StoreUrlResult:
        ...

    @abstractmethod
    def action_return_error(
        self,
        data: Any,
        format: Any,
    ) -> None:
        ...

    @abstractmethod
    def action_max_retries_exceeded_return_error(
        self,
        error_code: Any,
        message: Any,
        context: Any,
    ) -> None:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_short_url_is_created_and_stored(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_short_url_uses_generated_slug(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_slug_is_unique_in_database(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_required_inputs_validated(
        self,
    ) -> None:
        ...
