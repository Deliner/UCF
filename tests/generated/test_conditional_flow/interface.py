"""Generated interface for use case: test-conditional-flow.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any

from ucf.testing.framework_actions import FrameworkActions


@dataclass(frozen=True)
class StepAResult:
    value: int


class TestConditionalFlowInterface(ABC, FrameworkActions):
    """Interface with framework-provided actions inherited from FrameworkActions."""

    # ── State Setup (from requires) ──

    # ── Actions (from steps) ──

    @abstractmethod
    def action_step_a(
        self,
        threshold: Any,
    ) -> StepAResult:
        ...

    @abstractmethod
    def action_step_b(
        self,
        value: Any,
    ) -> None:
        ...

    @abstractmethod
    def action_step_c(
        self,
        value: Any,
    ) -> None:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_conditional_execution_works(
        self,
    ) -> None:
        ...
