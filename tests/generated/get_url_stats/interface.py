"""Generated interface for use case: get-url-stats.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any


@dataclass(frozen=True)
class FetchStatsResult:
    slug: str
    original_url: str
    total_clicks: int
    created_at: str
    created_by: str


class GetUrlStatsInterface(ABC):

    # ── State Setup (from requires) ──

    # ── Actions (from steps) ──

    @abstractmethod
    def action_fetch_stats(self, slug: Any) -> FetchStatsResult:
        ...

    @abstractmethod
    def action_render_stats(self, data: Any, format: Any) -> None:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_creator_sees_total_click_count(self) -> None:
        ...

    @abstractmethod
    def verify_creator_sees_original_url(self) -> None:
        ...

    @abstractmethod
    def verify_creator_sees_creation_timestamp(self) -> None:
        ...

    @abstractmethod
    def verify_required_inputs_validated(self) -> None:
        ...
