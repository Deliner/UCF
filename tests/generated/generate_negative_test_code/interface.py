"""Generated interface for use case: generate-negative-test-code.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any

from ucf.testing.framework_actions import FrameworkActions


@dataclass(frozen=True)
class LoaderContext:
    registry: Any
    loaded_count: int
    load_errors: list[Any]


@dataclass(frozen=True)
class ExtractErrorsResult:
    error_defs: list[Any]
    error_count: int


@dataclass(frozen=True)
class GenerateResult:
    negative_test_code: str
    error_methods: list[Any]
    test_count: int


class GenerateNegativeTestCodeInterface(ABC, FrameworkActions):
    """Interface with framework-provided actions inherited from FrameworkActions."""

    # ── State Setup (from requires) ──

    @abstractmethod
    def setup_loader(
        self,
        specs_dir: Any,
    ) -> LoaderContext:
        ...

    # ── Actions (from steps) ──

    @abstractmethod
    def action_extract_errors(
        self,
        usecase: Any,
        registry: Any,
    ) -> ExtractErrorsResult:
        ...

    @abstractmethod
    def action_generate(
        self,
        error_defs: Any,
        interface_class: Any,
        usecase_name: Any,
    ) -> GenerateResult:
        ...

    @abstractmethod
    def action_render_results(
        self,
        data: Any,
        format: Any,
    ) -> None:
        ...

    @abstractmethod
    def action_render_empty(
        self,
        data: Any,
        format: Any,
    ) -> None:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_for_each_action_error_a_testerror_class_is_generated(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_the_interface_has_abstract_error_methods_for_each_error(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_error_test_methods_receive_the_error_condition_as_a(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_generated_code_compiles_without_syntaxerror(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_required_inputs_validated(
        self,
    ) -> None:
        ...
