"""Generated interface for use case: visit-short-link.

DO NOT EDIT — this file is regenerated by `ucf generate`.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import Any

from ucf.testing.framework_actions import FrameworkActions


@dataclass(frozen=True)
class RedirectToDestinationResult:
    url: str
    success: bool


class VisitShortLinkInterface(ABC, FrameworkActions):
    """Interface with framework-provided actions inherited from FrameworkActions."""

    # ── State Setup (from requires) ──

    # ── Actions (from steps) ──

    @abstractmethod
    def action_redirect_to_destination(
        self,
        short_code: Any,
    ) -> RedirectToDestinationResult:
        ...

    @abstractmethod
    def action_show_404(
        self,
        short_code: Any,
    ) -> None:
        ...

    # ── Verifications (from postconditions + invariants) ──

    @abstractmethod
    def verify_visitor_is_redirected_to_original_page(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_visit_is_recorded_for_analytics(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_redirect_happens_in_200ms(
        self,
    ) -> None:
        ...

    @abstractmethod
    def verify_required_inputs_validated(
        self,
    ) -> None:
        ...
